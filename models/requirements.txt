# MT3 PyTorch Model Requirements
# Core PyTorch implementation for MT3 model

# Core ML framework
torch>=1.9.0,<3.0.0
numpy>=1.20.0,<2.0.0

# Optional audio processing (for full pipeline integration)
librosa>=0.8.0,<1.0.0
soundfile>=0.10.0

# Optional MIDI handling (for token decoding)
pretty_midi>=0.2.8
mido>=1.2.9

# Development and testing
pytest>=6.0.0
pytest-benchmark>=3.4.0

# Utilities
tqdm>=4.50.0
typing-extensions>=3.7.0

# For checkpoint conversion (if needed)
scipy>=1.5.0